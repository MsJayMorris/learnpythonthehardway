#This is making a variable with 4 strings ready to receive strings to print
formatter = "%r %r %r %r"

#This section passes in the variable strings for each formatter string (4)
print formatter % (1, 2, 3)
print formatter % ("one", "two", "three", "four")
print formatter % (True, False, False, True)
print formatter % (formatter, formatter, formatter, formatter)
print formatter % (
    "I had this thing",
    "That you could type up right.",
    "But it didn't sing.",
    "So I said goodnight."
    )
